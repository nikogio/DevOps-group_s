- var convert = function(epoch){  
-   var date = new Date(epoch*1000)
-   var formatted = date.getFullYear() + '-' + date.getMonth()+1 + '-' + date.getDate() + ' @ ' + date.getHours() + ':' + date.getMinutes() 
-   return formatted
- }

.timeline-box
    .title
        h2= path == '/public' ? "Public Timeline" : (path == '/' ? "My Timeline" : (messages[0].username + "'s Timeline"))


        if user
            if profile
                div(class="followstatus")
                    if user == profile.user_id
                        | This is you!
                    else if followed
                        | You are currently following this user. 
                        a(class="unfollow", href="/api/unfollow/#{profile.username}") Unfollow user.
                    else
                        | You are not yet following this user. 
                        a(class="follow", href="/api/follow/#{profile.username}") Follow user.
            else if path == '/'
                div(class="twitbox")
                    h3 What's on your mind #{user.username}?
                    form(action='' method='post')
                            input(type='text' name='text' size='60')
                            input(type='submit' value='Share')
            

        
        ul.messages 
            for msg in messages 
                li 
                    img(src='{{ message.email|gravatar(size=48) }}')
                    strong #[a(href="/api/#{msg.username}") #{msg.username}] 
                    span #{msg.text} 
                    small — #{convert(msg.pub_date)}
            else 
                li 
                    em There's no messages so far. 
            endfor 